services:
  crypto-web:
    build:
      context: ./WinFormsApp1
      dockerfile: Dockerfile
    container_name: do-an-crypto-web
    ports:
      - "5000:5000"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://0.0.0.0:5000
    restart: unless-stopped
